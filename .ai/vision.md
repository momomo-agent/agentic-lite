# agentic-lite — Vision

> 一个函数调用，拿到经过工具处理的结构化结果

## 一句话

比 raw API 多一点，比 LangChain 轻得多。

## 核心价值

开发者只需要 `npm install agentic-lite`，传入 provider/apiKey，一个函数调用就能拿到带搜索、代码执行、文件处理能力的 AI 回答。零框架概念，零配置负担。

## 不做什么

- 不做 agent 框架（没有 chain、graph、memory）
- 不做部署平台
- 不依赖任何重型运行时
