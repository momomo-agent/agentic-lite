# Design — m16: Public API Exports & Provider Docs

## Tasks

### task-1775572706464: Export ShellResult
Already done — `src/index.ts` exports `ShellResult` from `./types.js`. No code change needed.

### task-1775572719168: Export shell tool defs
Already done — `src/tools/index.ts` exports `shellToolDef` and `executeShell` from `./shell.js`. No code change needed.

### task-1775572728626: Document custom provider fallback
Requires updates to ARCHITECTURE.md and README.md. Submitted as CR (cannot modify directly).
