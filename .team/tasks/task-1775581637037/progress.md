# Shell exec browser safety — graceful error instead of crash

## Progress

- src/tools/shell.ts already has browser guard at line 30 — no code change needed
- Added Node.js-only note to README.md shell_exec section
