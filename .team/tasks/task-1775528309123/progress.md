# 实现 custom provider 钩子

## Progress

## Findings
- `createProvider()` already handles `provider='custom'` at `src/providers/provider.ts:53-55`
- Throws clear error when `customProvider` is absent
- No code changes needed — implementation is complete and correct as-is

