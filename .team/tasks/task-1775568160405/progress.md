# Fix ask() systemPrompt positional param

## Progress

Verified `ask(prompt, config)` signature matches spec. `systemPrompt` correctly passed via `config.systemPrompt`. No code change needed. Build passes.
