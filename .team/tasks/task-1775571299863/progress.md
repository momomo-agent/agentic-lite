# Fix AgenticResult.usage and images types

## Progress

- `usage` already required (no `?`) in types.ts:52
- `images` already `string[]` in types.ts:42
- No code changes needed — types already match spec
